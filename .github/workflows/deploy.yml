name: Deploy Viral Flux (Pages e Hostinger)

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Clonar o repositório
        uses: actions/checkout@v3

      # CORREÇÃO 1: Atualizado para Node 20 (exigido pelo seu Angular CLI)
      - name: Configurar Node.js 20
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      # CORREÇÃO 2: Mantendo o legacy-peer-deps para o erro de TypeScript
      - name: Instalar dependências
        run: npm install --legacy-peer-deps

      - name: Build do projeto
        run: npm run build

      # Passo de verificação: Mostra onde os arquivos foram criados
      # Angular às vezes cria dentro de dist/nome-do-projeto
      - name: Verificar pasta de saída
        run: ls -R dist/

      - name: Deploy para GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist/ # Se o comando acima mostrar uma subpasta, mude para dist/seu-projeto
          branch: gh-pages

      - name: Deploy FTP para Hostinger 📂
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST }}
          port: 21
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          local-dir: dist/ # Mesma observação da pasta acima
          server-dir: /
          dangerous-clean-slate: false
          dry-run: false
          log-level: verbose
